Testing suite scaffolding for TeacherOS.

This folder currently contains a minimal executable unit/api/security suite that aligns with:
- ARCHITECTURE.md (workflow + event-driven testing)
- AI_ARCHITECTURE.md (prompt injection guard + PII detector + run use case orchestration)

Run:
  pytest -q

