"""Top‑level package for the TeacherOS backend source tree.

Having an ``__init__`` file makes ``backend/src`` a proper Python package so that
relative imports (e.g., ``from .core import settings``) resolve correctly.
"""
